% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_dedup.R
\name{batch_dedup}
\alias{batch_dedup}
\title{Batch deduplication for large searches}
\usage{
batch_dedup(
  citations,
  batch_n = 50000,
  keep_source = NULL,
  keep_label = NULL,
  sort_by = c("year", "title", "author")
)
}
\arguments{
\item{citations}{A dataframe containing citation information.}

\item{batch_n}{Numeric value specifying the maximum number of citations per batch. Default is 50000.}

\item{keep_source}{Character vector specifying the citation source(s) to preferentially retain in the dataset as the unique record.}

\item{keep_label}{Character vector specifying the citation label(s) to preferentially retain in the dataset as the unique record.}

\item{sort_by}{Character vector specifying the sorting criteria. Default is c("year", "title","author"). Valid options are column names of the citations dataframe.}
}
\value{
A list with components:
\itemize{
\item unique - dataframe containing unique citations.
\item manual_dedup - dataframe containing citations to be manually checked for duplicates.
}
}
\description{
This function splits citations into batches, deduplicates each batch, and binds results together into one dataframe of unique citations.
}
\details{
The following fields will be used in \code{citations} (if provided): record_id, author, year, journal, doi, title, pages, volume, number, abstract, isbn, label, source
}
\examples{

# Perform batch deduplication
result <- batch_dedup(citations_df, batch_n = 250)

# View unique citations
head(result$unique)


}
